use the function  getcwd()( [[FUNCTION ALLOWED]]) to get the current path to the directory then use ft_strjoin to change this path and use chdir()( [[FUNCTION ALLOWED]]) to move into the wanted directory;

