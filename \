
#include "../includes/minishell.h"

int main()
{
//struct sigaction	sig;
//sigemptyset(&sig.sa_mask);
//init_signals(&sig);
	char *line;

	while (1)
	{
		line = readline(B_BLUE"\e279C "B_WHITE" mini"B_RED"shell "RESET);
			//parse(line);
		rl_on_new_line();
	}
	return (0);
}
